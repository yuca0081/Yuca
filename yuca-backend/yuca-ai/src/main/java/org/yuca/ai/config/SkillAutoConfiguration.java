package org.yuca.ai.config;

import org.springframework.context.annotation.Configuration;

/**
 * 技能系统自动配置
 */
@Configuration
public class SkillAutoConfiguration {
    // 技能系统的 Bean 会通过 @Component 自动扫描和注册
    // 这里保留用于未来的全局配置
}
