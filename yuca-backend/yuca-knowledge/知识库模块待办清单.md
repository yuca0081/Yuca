# 知识库模块待办清单（对标简历项目）

> 本文档对比简历"智慧工会知识库系统"项目（2025.10-2025.12）描述的能力与当前 `yuca-knowledge` 模块已实现功能，列出尚未实现或实现不完整的功能点，按优先级分类。
>
- 已实现功能汇总：参见 [知识库模块功能清单.md](./知识库模块功能清单.md)
- 对标基准：简历中描述的 6 大知识库工作项 + 职业技能中的 RAG 全流程能力

## 对标总览

| 简历功能 | 当前项目状态 | 优先级 |
|---|---|---|
| 文档删除时清理 MinIO 原文件 | ✅ 已完成（2026-07-21） | ~~P0~~ |
| 文档质量评分与智能路由（Clean/Decent/Garbage） | ❌ 未实现 | P2 |
| OCR 兜底（扫描件 PDF） | ❌ 未实现 | P2 |
| PDF / DOCX 文件解析 | ✅ 已完成（2026-07-20） | ~~P0~~ |
| 章节树建模（独占式切片 + 面包屑 + 行号 + 父子关系） | ✅ 已实现 | — |
| LLM 摘要做 embedding（短节点信号强） | ❌ 仅预留 `summary` 字段 | P1 |
| content 字段保留完整原文（长节点不稀释） | ❌ 当前是独占切片 | P1 |
| 三级检索流水线（向量 + BM25 + RRF + Cross-Encoder） | ✅ 已实现 | — |
| 保守父子注入（rerank 后取子节点扩展） | ❌ 未实现 | P1 |
| Token 预算控制（Lost in the middle） | ❌ 未实现 | P1 |
| 增量更新（SHA256 指纹对比） | ✅ 已完成（2026-07-21） | ~~P0~~ |
| 状态机（中断恢复、幂等重试） | ❌ 未实现 | P2 |
| 原子事务（旧软删→新插入→孤儿清理） | ⚠️ 仅单文档事务 | P1 |
| 查询扩展（Embedding 发现同义词扩展 BM25） | ❌ 未实现 | P1 |
| 多查询扩展（multi-query expansion） | ❌ 未实现 | P1 |
| 元数据过滤检索 | ❌ 未实现 | P1 |

---

## P0 - 基础能力缺失（影响生产可用性）

### 1. PDF / DOCX 文件解析修复 ✅ 已完成（2026-07-20）

**原问题**：`pom.xml` 已引入 `poi-ooxml` 和 `pdfbox`，但 `KnowledgeDocService.uploadDocument` 直接把文件字节当 UTF-8 字符串解析：

```java
// KnowledgeDocService.java:100
byte[] bytes = file.getBytes();
String content = new String(bytes, StandardCharsets.UTF_8);
```

PDF / DOCX 是二进制格式，按 UTF-8 解码会得到乱码，章节树构建会失败或产生无意义切片。

**实施结果**：

新增 4 个解析器文件（位于 `document/parser/`）：
- `DocumentParser.java` — `@FunctionalInterface`，定义 `String parse(byte[] bytes)`
- `MarkdownTxtParser.java` — 处理 md / txt，等价原 UTF-8 解码行为
- `PdfParser.java` — 基于 PDFBox 2.0.29，`PDDocument.load` + `PDFTextStripper`，加密 PDF 直接抛 `IOException`
- `DocxParser.java` — 基于 POI 5.2.5，按 `IBodyElement` 顺序遍历段落和表格，表格转纯文本时单元格用 ` | ` 分隔
- `DocumentParserRegistry.java` — 按扩展名路由，未知扩展名降级到 MarkdownTxtParser 并记 WARN

修改的文件：
- `config/KnowledgeConfig.java` — 新增 `documentParserRegistry` Bean，注册 md/txt/pdf/docx 四个扩展名
- `service/KnowledgeDocService.java` — 注入 `DocumentParserRegistry`，把 `new String(bytes, UTF_8)` 替换为 `registry.parse(fileFormat, bytes)`；顺手修复原 `catch (IOException) → throw RuntimeException` 的异常吞掉问题，改为统一 `BusinessException`

**范围说明**：
- 不含 OCR 兜底（扫描件 PDF 仍返回空文本，留给 P2-#12）
- 不含 `.doc` 老版二进制格式（需 HWPFDocument）
- 表格在 DOCX 中以纯文本形式拼接，无 H1-H6 标题，自然走字符切片降级路径

**验证状态**：因 `yuca-ai/SummaryEnhancer.java` 在研代码编译错误阻塞，**未跑 mvn compile**。需待该文件修复后补一次全量编译验证。

---

### 2. 文档增量更新（SHA256 指纹对比） ✅ 已完成（2026-07-21）

**原问题**：每次上传都走"新建"流程，无去重和更新逻辑。用户重新上传同名文档会让旧切片和新切片并存，检索结果被旧版本污染；误重复上传则浪费 embedding 成本。

**实施结果**：

新增字段：
- DDL `yuca-app/src/main/resources/table/knowledge/knowledge_doc.sql` 增加 `content_hash VARCHAR(64)` + `idx_doc_hash` 普通索引 + 表/列注释
- 实体 `entity/KnowledgeDoc.java` 增加 `private String contentHash;`

三分支路由（`KnowledgeDocService.uploadDocument`）：
- **分支 A（hash 完全相同）**：跳过解析/上传/嵌入，直接返回旧 docId。日志 `[upload] hash 相同，跳过上传`
- **分支 B（首次上传）**：走原新建流程。日志 `[upload] 新建文档`
- **分支 C（hash 不同）**：触发重建——同一 `@Transactional` 内软删旧 doc + 软删旧切片 + 上传 MinIO（同路径覆盖）+ 插入新 doc（带 hash）+ 插入新切片。日志 `[upload] 检测到内容变更，触发重建`

新增 3 个私有方法：
- `sha256Hex(byte[])` — 基于 `HexFormat`（Java 17+），比 `String.format("%02x")` 快一个数量级
- `findActiveByName(kbId, docName)` — 用 LambdaQueryWrapper + `selectOne`，依赖 `@TableLogic` 自动过滤 deleted=1
- `softDeleteChunksOfDoc(docId)` — 用 `knowledgeChunkMapper.delete(wrapper)`，`@TableLogic` 自动转 `UPDATE SET deleted=1`

**关键决策**：
- 识别口径用 `(kbId, docName)` 而非全局 hash，符合用户视角
- 顺序优化：先算 hash → 查旧 → 决定是否解析，hash 相同时完全跳过解析成本
- MinIO 文件覆盖（同路径上传），不保留备份——节省存储，旧 DB 记录仍支持逻辑审计
- 接口零变更：返回值仍为 `Long` docId，前端无感

**范围说明**：
- 不含显式 `reupload` 接口（未来扩展）
- 不含 MinIO 旧文件清理逻辑（留给 P0-#3）
- 不含状态机/异步重建（留给 P2-#13）
- 不含 `(kbId, docName)` 数据库唯一约束（并发上传仍可能产生重复）

**遗留风险**：
- `findActiveByName` 用 `selectOne`，若历史数据已存在多条未删除同名记录会抛 `TooManyResultsException`。这是有意为之——让数据污染显式暴露而非默默选第一条
- 重建同步执行，大文档耗时长（嵌入 30s+），异步化留给 P2-#13
- 进程崩溃在重建中途：MinIO 已是新文件但 DB 可能保留旧记录的 deleted=0，下次上传会再次"重建"——可接受

---

### 3. 文档删除时清理 MinIO 原文件 ✅ 已完成（2026-07-21）

**原问题**：`KnowledgeDocService.deleteDocuments` 只做 DB 软删，MinIO 物理文件永久残留，造成存储成本累积。

**实施结果**：

修改 `service/KnowledgeDocService.java`：

- `deleteDocuments` 在 DB 软删 + 切片软删之后，调用 `deleteMinioFileSafely(doc.getFilePath(), docId)`
- 新增私有方法 `deleteMinioFileSafely`：
  - 判空（`file_path` 为 NULL 或 blank 时跳过 + DEBUG 日志）
  - try-catch 包裹 `fileStorageService.delete()`（任何异常仅记 WARN 日志，不抛出）
  - 失败残留文件留作 P2-#13 状态机时的后台清理 Job 兜底

**关键决策**：
- 顺序：DB 软删 → MinIO 删。MinIO 失败时 DB 已成功，业务功能正常
- 失败隔离：MinIO 异常不影响事务，最终一致即可
- 复用 `FileStorageService.delete`（接口已存在，`MinioFileStorageService` 的 `removeObject` 幂等）
- 不动 Controller / Mapper / DDL

**范围说明**：
- 重建场景的 MinIO 清理不处理（P0-#2 已用同路径覆盖语义）
- 不引入 `@TransactionalEventListener` 事务后置（保持简单）
- 不引入异步孤儿清理 Job（留给 P2-#13）

**遗留风险**：
- DB 事务提交前 MinIO 已删，若事务回滚会丢失文件——概率极低，且 P0-#2 增量更新可恢复
- for 循环部分成功部分失败——当前实现已有行为，未改变

---

## P1 - 检索精度提升（简历亮点功能）

### 4. LLM 摘要做 embedding

**现状**：`ChapterNode.embeddingText()` 把 `title + breadcrumb + content` 拼接做 embedding。短节点（如只有标题的目录章节）信号弱，长节点（千行正文）会稀释语义。

`knowledge_chunk.summary` 字段已在表结构里预留，但 v1 未填。

**目标**：节点入库前调用 LLM 生成摘要（≤200 字），存入 `summary` 字段，**用摘要而非原文做 embedding**，`content` 字段保留完整原文用于检索结果展示。

**涉及文件**：
- 修改 `service/KnowledgeDocService.java`：`saveChapterNodes` 中插入前调 LLM 生成 summary
- 修改 `document/ChapterNode.java`：新增 `summary` 字段，`embeddingText()` 改为优先用 summary
- 依赖 `yuca-ai` 的 LLM 客户端（单条文本摘要）

**注意**：摘要调用有成本，需考虑批量化（如多个节点合并一次 prompt）。

---

### 5. content 字段保留完整原文

**现状**：独占式切片下，`content` 只含父节点引言（不含子节点正文），检索命中的是父节点时返回的内容会偏少。

**目标**：`content` 改为存 **节点完整正文（含所有子孙内容）**，但 `embedding` 仍基于独占式切片或摘要生成，避免信号重复。检索结果展示时可直接返回 content，无需二次拼装。

**涉及文件**：
- 修改 `document/ChapterNode.java`：新增 `fullContent` 字段（区别于 `content`）
- 修改 `document/MarkdownChapterTreeBuilder.java`：节点构造时填充 `fullContent`（合并到下一任意级别标题前的所有行）
- 修改 `service/KnowledgeDocService.java`：`insertNodeRecursive` 时写入 `content = fullContent`

---

### 6. 保守父子注入

**现状**：rerank 后直接截取 topN 返回。父子结构信息未被利用。

**目标**：rerank 后对每个高分节点，从章节树 `parent_id` 反向查最多 2 个子节点追加到候选结果，子节点得分 = 父节点得分 × 0.5 折扣。最后统一排序截取 topN。

**涉及文件**：
- 修改 `mapper/KnowledgeChunkMapper.java`：新增 `selectChildrenByParentId(parentId, limit)`
- 修改 `resources/mapper/knowledge/KnowledgeChunkMapper.xml`：补 SQL
- 修改 `service/KnowledgeRetrievalService.java`：rerank 之后、limit 之前插入"父子注入"阶段

---

### 7. Token 预算控制

**现状**：检索结果按"数量"（topN）截取，长节点和短节点混合可能让 LLM 上下文爆掉或被截断（Lost in the middle）。

**目标**：按 token 预算（如 4000 tokens）而非数量截取；单节点超长时按 **"摘要 → 开头 → 结尾 → 中间截断"** 优先级保留。

**涉及文件**：
- 新建 `service/TokenBudgetAssembler.java`（拼装最终上下文）
- 依赖 tokenizer（如 jtokkit 或 LangChain4j 的 `TokenUsageEstimator`）
- 修改 `service/KnowledgeRetrievalService.java`：返回 `RetrievedChunk` 列表前过一遍 assembler

---

### 8. 查询扩展（同义词自动发现）

**现状**：BM25 路用原始 query 做 `plainto_tsquery`，无法覆盖同义词（如查"裁员"找不到"裁员优化"）。

**目标**：基于 Embedding 从预设词汇表（或近期高频查询）中找 query 的 top-K 近义词，扩展后的 query 喂给 BM25 路；向量检索路保持原始 query（语义捕获已足够）。零维护成本——换知识库只需 rebuild 词汇表。

**涉及文件**：
- 新建 `service/QueryExpansionService.java`（输入 query → 输出扩展 query）
- 新建 `service/VocabularyStore.java`（词汇表 embedding 索引，可用 Redis 或 PG 表）
- 修改 `service/KnowledgeRetrievalService.java`：BM25 路用扩展 query

---

### 9. 多查询扩展（Multi-Query Expansion）

**现状**：单 query 召回，视角单一。

**目标**：调 LLM 把用户原始 query 改写为 3-5 个不同视角的子 query，每个子 query 都做向量+BM25 召回，结果 RRF 融合后统一 rerank。简历"职业技能"中明确提及。

**涉及文件**：
- 新建 `service/MultiQueryExpander.java`（调 LLM 改写）
- 修改 `service/KnowledgeRetrievalService.java`：循环多 query 召回 → 融合

**与 #8 关系**：#8 是零成本同义词扩展（基于 embedding），#9 是 LLM 改写（更智能但有 token 成本），两者可叠加。

---

### 10. 元数据过滤检索

**现状**：检索只能按 `kbId` 过滤，无法按文档属性（如时间范围、标签、文档来源）细粒度筛选。

**目标**：
- `knowledge_doc` / `knowledge_chunk` 增加业务元数据（如 `tags`、`effective_date`、`source`）
- 检索接口支持元数据过滤参数（如 `?source=official&tag=政策`）
- 向量检索 SQL 和 BM25 检索 SQL 都拼接过滤条件

**涉及文件**：
- 修改 DDL：`knowledge_doc.sql` 增加 `tags VARCHAR[]`、`metadata JSONB`（已有 metadata 但未使用）
- 修改 `dto/request/SemanticSearchRequest.java`：增加过滤参数
- 修改 `mapper/KnowledgeChunkMapper.xml`：search SQL 加 join + filter

---

## P2 - 健壮性与工程化

### 11. 文档质量评分与智能路由

**现状**：所有文档走同一套切片策略（md 章节树，其他字符切片），扫描件 / OCR 质量差的 PDF 会产出大量无意义切片。

**目标**：对解析后的纯文本做质量评分（基于提取率、OCR 瑕疵密度、格式一致性），分三级路由：
- **Clean**（高质量）→ 章节树切片
- **Decent**（中等）→ 段落 + 固定长度混合切片
- **Garbage**（低质量）→ OCR 兜底后重新评分

**涉及文件**：
- 新建 `service/DocumentQualityScorer.java`
- 新建 `service/DocumentRoutingService.java`（策略模式）
- 修改 `service/KnowledgeDocService.java`：上传流程接入评分 + 路由

---

### 12. OCR 兜底（扫描件 PDF）

**现状**：PDFBox 对扫描件（图片型 PDF）只能提取空文本。

**目标**：PDFBox 提取结果为空或质量分过低时，调用 OCR（如 PaddleOCR、Tesseract 或阿里云 OCR API）重新提取。

**涉及文件**：
- 新建 `document/parser/OcrParser.java`（封装 OCR 调用）
- 修改 `document/parser/PdfParser.java`：空文本降级到 OcrParser
- 新增配置项 `yuca.knowledge.ocr.enabled` / `ocr.provider`

---

### 13. 上传状态机（中断恢复 + 幂等重试）

**现状**：上传流程在一个 `@Transactional` 方法里完成，解析/嵌入任一步骤失败会整体回滚，但已上传到 MinIO 的文件残留；大文档嵌入耗时长（>30s），前端无进度反馈。

**目标**：
- `knowledge_doc` 增加 `process_status` 字段（`UPLOADING / PARSING / EMBEDDING / READY / FAILED`）
- 每个阶段独立事务，失败后可从断点恢复
- 前端 SSE 或轮询获取进度

**涉及文件**：
- 修改 DDL：`knowledge_doc.sql` 增加 `process_status VARCHAR(20)`
- 新建 `service/DocumentProcessingStateMachine.java`
- 修改 `service/KnowledgeDocService.java`：拆分为阶段方法

---

### 14. 原子事务提交补全

**现状**：单文档上传已在事务内；但文档重建（增量更新场景）需要"旧切片软删 → 新切片插入 → 孤儿清理"严格原子。

**目标**：与 #2 增量更新绑定，三步操作置于同一事务，确保任一失败可回滚。孤儿清理针对无父节点的 chunk（如重建过程中失败残留）。

**涉及文件**：
- 与 #2 同批改造
- 新增 `mapper/KnowledgeChunkMapper.xml` 中 `cleanupOrphans()` SQL

---

## 待办优先级建议

**P0 全部完成** ✅ — #1 PDF/DOCX 解析（2026-07-20）、#2 增量更新（2026-07-21）、#3 MinIO 清理（2026-07-21）。基础能力已具备生产门槛。

**中期（3-4 周）**：P1 中的 #4、#5、#6、#7 — 简历中明确写明的章节树增强与检索精度优化，对面试讲述有强支撑。

**长期**：P1 中的 #8、#9、#10 和 P2 全部 — 锦上添花，可作为后续迭代。

## 实施约束

- 所有改动需保持 `RetrievalService` 接口（`yuca-ai` 中定义）不变，下游 `yuca-assistant` 无感
- 新增 LLM 调用（摘要、多查询扩展）需走 `AgentFactory` 或直接复用 `EmbeddingService` 的客户端配置
- DDL 变更同步更新 `yuca-app/src/main/resources/table/knowledge/*.sql` 中的对应文件，保持 DDL 与实体一致
- 切片相关改动需考虑存量数据迁移（如有线上数据），建议在 DDL 中预留兼容字段并保留旧字段一段时间
